# Presentation-channel QA evidence

These PNGs are deterministic off-screen renders of the production
`CoachingPresentationView` using `CoachingEvent.sample`, dark appearance, and the
production panel sizes. They are generated by an XCTest rather than by a duplicate
mock view.

Run `scripts/generate-presentation-evidence.sh` to rebuild them. The generator
also verifies that every image is a non-empty PNG at the expected point size.

The six renders are also available as an 18-second [Clipy proof recording](https://clipy.online/video/92v3fdehha6k)
with [agent-readable context](https://clipy.online/video/92v3fdehha6k.md).

## Visual channels

| Channel | Evidence | Runtime contract |
| --- | --- | --- |
| Top-right Toast | [PNG](topRightToast.png) | 360×92 pt, top-right, 4 seconds |
| Top-center Shelf | [PNG](topCenterShelf.png) | 500×112 pt, top-center, 4 seconds |
| Cursor Halo | [PNG](cursorHalo.png) | 120×120 pt, centered on event pointer, 4 seconds |
| Pointer Card | [PNG](pointerCard.png) | 320×92 pt, clamped beside event pointer, 4 seconds |
| Status Feedback | [PNG](statusFeedback.png) | 300×76 pt, top-center, 4 seconds; ellipsis changes to success after 700 ms |
| Decision Banner | [PNG](decisionBanner.png) | 700×128 pt, top-center, 8 seconds, explicit dismiss buttons |

The geometry/timing test also proves that panels join every Space and are allowed
over full-screen spaces. Pointer-based presentations now choose the display that
contains the event pointer; the multi-display coordinate conversion and placement
are covered with synthetic two-display geometry.

## Combination decision

- Top-right Toast combines safely with either pointer-based channel.
- Cursor Halo and Pointer Card are redundant at the pointer; enable at most one.
- Top-center Shelf, Status Feedback, and Decision Banner share the top-center
  anchor and overlap. Enable at most one of these three until the product gains a
  stacking policy.
- Native banner, Dock badge, Dock bounce, and sound are OS-controlled and can be
  combined with one custom visual channel, subject to user notification and sound
  preferences.

## What this evidence does not claim

Off-screen rendering cannot prove Notification Center authorization or appearance,
the physical Dock badge/bounce, audible sound output, behavior on a real full-screen
Space, or keyboard dismissal of a nonactivating panel. Those require a signed app
running in a controlled macOS QA account and screen/audio capture. The current
implementation also lacks a verified keyboard-dismissal path. Issue #11 must remain
open until those runtime checks pass or their acceptance criteria are deliberately
changed.
