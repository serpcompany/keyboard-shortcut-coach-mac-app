# Keylume Clone privacy

Keylume Clone reads accessibility-exposed application menus and observes shortcut-related keyboard and mouse events locally to provide its overlay, coaching, and analytics features.

Usage records remain on this Mac under the user's Application Support directory. The app does not transmit usage records, keystrokes, application names, or menu contents. The optional update checker makes a request only when a candidate-specific update feed is explicitly configured.
